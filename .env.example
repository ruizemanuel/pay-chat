# LLM provider keys (server-only; not exposed to the browser)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# thirdweb (for x402 facilitator + server wallet)
THIRDWEB_SECRET_KEY=
# hex-encoded private key of the server wallet that signs settle operations
SERVER_WALLET_PK=

# Celo RPC — https://forno.celo.org for mainnet, https://forno.celo-sepolia.celo-testnet.org for sepolia
CELO_RPC=https://forno.celo.org

# Contract address (filled after deploy)
NEXT_PUBLIC_PROMPT_RECEIPT_ADDRESS=

# Celoscan API key (contract verification)
CELOSCAN_API_KEY=
